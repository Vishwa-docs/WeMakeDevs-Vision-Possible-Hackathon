# WorldLens - Vision Agents Backend Dependencies
# Install: pip install -r requirements.txt

# Core Vision Agents SDK with all needed plugins
vision-agents[getstream,gemini,ultralytics,moondream,nvidia,huggingface]

# Environment management
python-dotenv>=1.0.0

# OpenCV for camera/video handling (headless avoids libavdevice conflict with av/PyAV)
opencv-python-headless>=4.8.0

# Google MediaPipe — hand landmark detection for SignBridge
mediapipe>=0.10.20

# Database
aiosqlite>=0.19.0

# HTTP client (for Maps API, etc.)
httpx>=0.25.0

# Async HTTP client (for weather API)
aiohttp>=3.9.0

# Logging and utilities
structlog>=23.0.0
